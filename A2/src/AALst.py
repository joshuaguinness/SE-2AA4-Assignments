# Created by Joshua Guinness

from StdntAllocTypes import *

class StdntAllocTypes:

	def __init__(self):
		self.s = []

		for Dept in (DepT):
			self.s.append((Dept, []))

	def add_stdnt(dep, m):
		for i in self.s
			if (i[0] == dep):
				i[1].append(m)

	def lst_alloc(d):
		for i in self.s:
			if (i[0] == d):
				return i[1]

	def num_alloc(d):
		for i in self.s:
			if (i[0] == d):
				return len(i[1])